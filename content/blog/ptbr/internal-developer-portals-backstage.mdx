---
title: 'Internal Developer Portals: Como o Backstage Está Revolucionando a Developer Experience'
description: 'Descubra como Internal Developer Portals como o Backstage melhoram a produtividade, reduzem carga cognitiva e aceleram o onboarding de desenvolvedores.'
author: 'Equipe Integr8'
date: 2025-01-10
category: 'Developer Experience'
tags: ['backstage', 'developer-portal', 'developer-experience', 'platform-engineering', 'spotify', 'devops']
read_time: 11
featured: true
---

import { Callout, FeatureGrid, Steps, PlantUML, SimpleTimeline } from '@components/content';

## O Problema da Fragmentação de Ferramentas

Em organizações modernas, desenvolvedores precisam navegar entre dezenas de ferramentas diariamente: GitHub, Jira, Jenkins, ArgoCD, Grafana, PagerDuty, Confluence... Essa fragmentação gera **carga cognitiva excessiva** e reduz a produtividade.

<Callout type="warning" title="O Custo Oculto">
  Desenvolvedores perdem em média **2-3 horas por dia** buscando informações espalhadas em diferentes sistemas. Isso representa ~30% do tempo produtivo desperdiçado.
</Callout>

## O que é um Internal Developer Portal?

Um Internal Developer Portal (IDP) é uma plataforma centralizada que:

<FeatureGrid
  features={[
    {
      icon: '📚',
      title: 'Catálogo de Serviços',
      description: 'Inventário completo de todos os serviços, APIs, bibliotecas e recursos da organização',
    },
    {
      icon: '📝',
      title: 'Documentação Centralizada',
      description: 'Docs técnicos, ADRs, runbooks e guias em um único lugar pesquisável',
    },
    {
      icon: '🚀',
      title: 'Self-Service',
      description: 'Templates para criar novos projetos, provisionar infraestrutura e configurar pipelines',
    },
    {
      icon: '🔗',
      title: 'Integração de Ferramentas',
      description: 'Conecta todas as ferramentas do ecossistema em uma interface unificada',
    },
  ]}
  columns={2}
/>

## Backstage: O Padrão da Indústria

Criado pelo Spotify e doado à CNCF, o Backstage se tornou o padrão de facto para Internal Developer Portals.

### Arquitetura do Backstage

<PlantUML
  code={`
@startuml
skinparam dpi 300

skinparam rectangleBorderColor #5B8DD9
skinparam rectangleBackgroundColor #F7FAFC

title Arquitetura do Backstage

rectangle "**Backstage App**" as app #E8F4FD {
  rectangle "Catalog" as catalog #D1FAE5
  rectangle "Scaffolder" as scaffolder #D1FAE5
  rectangle "TechDocs" as techdocs #D1FAE5
  rectangle "+ Plugins" as plugins #FEF3C7
}

rectangle "**Backend Services**" as backend #E8F4FD

rectangle "PostgreSQL" as pg #F7FAFC
rectangle "Search\\n(Elasticsearch)" as search #F7FAFC
rectangle "Auth\\nProvider" as auth #F7FAFC

app -down-> backend
backend -down-> pg
backend -down-> search
backend -down-> auth

@enduml
  `}
  alt="Arquitetura do Backstage"
  caption="Backstage: Catalog, Scaffolder, TechDocs + Plugins sobre Backend Services"
/>

### Componentes Principais

<Steps
  steps={[
    {
      title: 'Software Catalog',
      description: 'Registro de todos os componentes da organização com metadados, owners e dependências.',
    },
    {
      title: 'Software Templates (Scaffolder)',
      description: 'Templates para criar novos projetos seguindo padrões da organização (Golden Paths).',
    },
    {
      title: 'TechDocs',
      description: 'Documentação como código que vive junto com o repositório e é renderizada automaticamente.',
    },
    {
      title: 'Plugins Ecosystem',
      description: 'Extensível via plugins para Kubernetes, CI/CD, observability, segurança e mais.',
    },
  ]}
/>

## Implementando o Software Catalog

O coração do Backstage é o catálogo de software, definido via arquivos YAML:

```yaml
# catalog-info.yaml no repositório do serviço
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: payments-api
  description: API de processamento de pagamentos
  tags:
    - python
    - fastapi
    - payments
  annotations:
    github.com/project-slug: myorg/payments-api
    pagerduty.com/service-id: PXXXXXX
    grafana/dashboard-selector: "app=payments-api"
spec:
  type: service
  lifecycle: production
  owner: team-payments
  system: checkout-system
  dependsOn:
    - component:users-api
    - resource:payments-database
  providesApis:
    - payments-api
---
apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: payments-api
  description: API REST para processamento de pagamentos
spec:
  type: openapi
  lifecycle: production
  owner: team-payments
  definition:
    $text: ./openapi.yaml
```

## Software Templates para Golden Paths

Templates permitem criar novos projetos padronizados:

```yaml
# template.yaml
apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata:
  name: python-service
  title: Serviço Python com FastAPI
  description: Cria um novo microserviço Python com FastAPI, Docker, CI/CD e observability
  tags:
    - python
    - fastapi
    - recommended
spec:
  owner: platform-team
  type: service

  parameters:
    - title: Informações do Serviço
      required:
        - name
        - owner
      properties:
        name:
          title: Nome do Serviço
          type: string
          pattern: '^[a-z0-9-]+$'
        description:
          title: Descrição
          type: string
        owner:
          title: Time Owner
          type: string
          ui:field: OwnerPicker

    - title: Configurações Técnicas
      properties:
        database:
          title: Banco de Dados
          type: string
          enum:
            - postgresql
            - mysql
            - none
          default: postgresql

  steps:
    - id: fetch-template
      name: Fetch Template
      action: fetch:template
      input:
        url: ./skeleton
        values:
          name: ${{ parameters.name }}
          owner: ${{ parameters.owner }}
          database: ${{ parameters.database }}

    - id: create-repo
      name: Create Repository
      action: publish:github
      input:
        repoUrl: github.com?owner=myorg&repo=${{ parameters.name }}
        description: ${{ parameters.description }}

    - id: register-catalog
      name: Register in Catalog
      action: catalog:register
      input:
        repoContentsUrl: ${{ steps['create-repo'].output.repoContentsUrl }}
        catalogInfoPath: /catalog-info.yaml

  output:
    links:
      - title: Repository
        url: ${{ steps['create-repo'].output.remoteUrl }}
      - title: Open in Backstage
        icon: catalog
        entityRef: ${{ steps['register-catalog'].output.entityRef }}
```

<Callout type="success" title="Resultado">
  Com templates bem definidos, um novo desenvolvedor pode criar um serviço production-ready em **menos de 5 minutos**, em vez de dias copiando configurações manualmente.
</Callout>

## Plugins Essenciais

### Kubernetes Plugin

```tsx
// Visualizar pods, deployments e logs diretamente no Backstage
import { EntityKubernetesContent } from '@backstage/plugin-kubernetes';

// No EntityPage.tsx
<EntityLayout.Route path="/kubernetes" title="Kubernetes">
  <EntityKubernetesContent />
</EntityLayout.Route>
```

### CI/CD Integration

```yaml
# Mostrar status de builds no catálogo
annotations:
  github.com/project-slug: myorg/my-service
  # ou
  circleci.com/project-slug: github/myorg/my-service
  # ou
  jenkins.io/job-full-name: folder/my-service
```

### Observability

```yaml
annotations:
  grafana/dashboard-selector: "app=my-service"
  prometheus.io/scrape-target: "my-service:9090"
  sentry.io/project-slug: my-service
```

## TechDocs: Documentação como Código

```yaml
# mkdocs.yml no repositório
site_name: Payments API
nav:
  - Home: index.md
  - Architecture: architecture.md
  - API Reference: api-reference.md
  - Runbook: runbook.md

plugins:
  - techdocs-core
```

```markdown
<!-- docs/index.md -->
# Payments API

## Overview

A Payments API é responsável por processar todas as transações
financeiras da plataforma.

## Quick Start

\`\`\`bash
# Clone o repositório
git clone https://github.com/myorg/payments-api

# Instale dependências
pip install -r requirements.txt

# Execute localmente
uvicorn main:app --reload
\`\`\`

## Architecture Decision Records

- [ADR-001: Escolha do FastAPI](adr/001-fastapi.md)
- [ADR-002: Padrão de Retry](adr/002-retry-pattern.md)
```

## Métricas de Sucesso

Como medir o impacto do seu IDP:

<FeatureGrid
  features={[
    {
      icon: '⏱️',
      title: 'Time to First Commit',
      description: 'Tempo do onboarding ao primeiro commit em produção',
    },
    {
      icon: '🔍',
      title: 'Search Success Rate',
      description: 'Percentual de buscas que encontram a informação desejada',
    },
    {
      icon: '📊',
      title: 'Catalog Completeness',
      description: 'Percentual de serviços documentados no catálogo',
    },
    {
      icon: '😊',
      title: 'Developer Satisfaction',
      description: 'NPS e surveys regulares sobre a experiência',
    },
  ]}
  columns={4}
/>

## Roadmap de Implementação

<SimpleTimeline
  title="Jornada de Adoção do Backstage"
  items={[
    {
      label: 'Fase 1',
      title: 'MVP: Catálogo',
      description: 'Comece importando todos os repositórios e definindo ownership. Valor imediato.',
      status: 'completed',
      icon: 'document',
    },
    {
      label: 'Fase 2',
      title: 'Documentação',
      description: 'Ative TechDocs e migre documentação existente para docs-as-code.',
      status: 'completed',
      icon: 'document',
    },
    {
      label: 'Fase 3',
      title: 'Templates',
      description: 'Crie 2-3 templates para os tipos mais comuns de projetos na organização.',
      status: 'current',
      icon: 'cog',
    },
    {
      label: 'Fase 4',
      title: 'Integrações',
      description: 'Adicione plugins para CI/CD, Kubernetes, observability conforme necessidade.',
      status: 'pending',
      icon: 'server',
    },
    {
      label: 'Fase 5',
      title: 'Self-Service Avançado',
      description: 'Provisioning de infraestrutura, criação de ambientes, gestão de secrets.',
      status: 'pending',
      icon: 'rocket',
    },
  ]}
/>

<Callout type="info" title="Dica de Adoção">
  A adoção do Backstage é mais efetiva quando você resolve uma **dor real** dos desenvolvedores primeiro. Identifique o maior problema e comece por ali.
</Callout>

---

*Quer implementar um Internal Developer Portal na sua organização? [Fale com nossos especialistas](/ptbr/contato) em Developer Experience.*
