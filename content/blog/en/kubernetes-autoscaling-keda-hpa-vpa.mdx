---
title: 'Kubernetes Autoscaling: HPA, VPA, and KEDA for Scaling Workloads'
description: 'Complete guide to Kubernetes autoscaling with HPA, VPA, and KEDA. Learn to scale based on custom metrics, events, and optimize resources.'
author: 'Integr8 Team'
date: 2025-01-13
category: 'Kubernetes'
tags: ['kubernetes', 'autoscaling', 'hpa', 'vpa', 'keda', 'cost-optimization']
read_time: 14
featured: true
---

import { Callout, FeatureGrid, Steps, PlantUML } from '@components/content';

## Why Autoscaling?

In cloud-native environments, demand varies constantly. Without autoscaling, you choose between wasting resources (over-provisioning) or having unavailability (under-provisioning).

<FeatureGrid
  features={[
    {
      icon: '💰',
      title: 'Cost Reduction',
      description: 'Automatically scale down during low demand periods',
    },
    {
      icon: '⚡',
      title: 'Performance',
      description: 'Scale up to maintain latency and throughput under load',
    },
    {
      icon: '🔄',
      title: 'Resilience',
      description: 'Automatic load distribution across replicas',
    },
    {
      icon: '🎯',
      title: 'Efficiency',
      description: 'Automatic right-sizing of resources (CPU/memory)',
    },
  ]}
  columns={2}
/>

## Types of Autoscaling in Kubernetes

<PlantUML
  code={`
@startuml
skinparam dpi 300

skinparam rectangleBorderColor #5B8DD9
skinparam componentBackgroundColor #E8F4FD
skinparam componentBorderColor #5B8DD9

title Kubernetes Autoscaling

rectangle "Pod Autoscaling" as podscale {
  component "**HPA**\\n(Horizontal)" as hpa #E8F4FD
  component "**VPA**\\n(Vertical)" as vpa #FEF3C7
  component "**KEDA**\\n(Event-driven)" as keda #D1FAE5
}

rectangle "Node Autoscaling" as nodescale #FEE2E2 {
  component "**Cluster Autoscaler**\\n(± Nodes in cluster)" as ca
}

note bottom of hpa
  ± Replicas
end note

note bottom of vpa
  ± CPU/Memory
end note

note bottom of keda
  ± Replicas
  (0 to N)
end note

podscale -[hidden]down-> nodescale

@enduml
  `}
  alt="Types of Autoscaling in Kubernetes"
  caption="HPA scales horizontally, VPA vertically, KEDA by events, and Cluster Autoscaler manages nodes"
/>

## HPA: Horizontal Pod Autoscaler

HPA adjusts the number of replicas based on metrics.

### Basic HPA with CPU

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: myapp-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: myapp
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300  # Wait 5min before scale down
      policies:
        - type: Percent
          value: 10
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0  # Immediate scale up
      policies:
        - type: Percent
          value: 100
          periodSeconds: 15
        - type: Pods
          value: 4
          periodSeconds: 15
      selectPolicy: Max
```

<Callout type="info" title="Requests Are Essential">
  HPA calculates utilization based on `requests`, not `limits`. If you don't define requests, HPA won't work correctly.
</Callout>

### HPA with Custom Metrics

To scale based on application metrics (latency, queue, etc.):

```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: myapp-hpa-custom
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: myapp
  minReplicas: 2
  maxReplicas: 20
  metrics:
    # Prometheus Adapter metric
    - type: Pods
      pods:
        metric:
          name: http_requests_per_second
        target:
          type: AverageValue
          averageValue: "1000"

    # External metric (e.g., SQS queue length)
    - type: External
      external:
        metric:
          name: sqs_messages_visible
          selector:
            matchLabels:
              queue: myapp-queue
        target:
          type: AverageValue
          averageValue: "30"
```

### Prometheus Adapter for Custom Metrics

```yaml
# prometheus-adapter-config
rules:
  - seriesQuery: 'http_requests_total{namespace!="",pod!=""}'
    resources:
      overrides:
        namespace: {resource: "namespace"}
        pod: {resource: "pod"}
    name:
      matches: "^(.*)_total$"
      as: "${1}_per_second"
    metricsQuery: 'sum(rate(<<.Series>>{<<.LabelMatchers>>}[2m])) by (<<.GroupBy>>)'
```

## VPA: Vertical Pod Autoscaler

VPA automatically adjusts CPU and memory requests for pods.

### VPA Installation

```bash
git clone https://github.com/kubernetes/autoscaler.git
cd autoscaler/vertical-pod-autoscaler
./hack/vpa-up.sh
```

### VPA Modes

<Steps
  steps={[
    {
      title: 'Off',
      description: 'Recommendations only, no automatic action. Good for starting.',
    },
    {
      title: 'Initial',
      description: 'Sets requests only at pod creation. Does not modify existing pods.',
    },
    {
      title: 'Auto',
      description: 'Updates running pods (causes restart). Most aggressive mode.',
    },
  ]}
/>

```yaml
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: myapp-vpa
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: myapp
  updatePolicy:
    updateMode: "Auto"  # Off, Initial, or Auto
  resourcePolicy:
    containerPolicies:
      - containerName: myapp
        minAllowed:
          cpu: 100m
          memory: 128Mi
        maxAllowed:
          cpu: 4
          memory: 8Gi
        controlledResources: ["cpu", "memory"]
        controlledValues: RequestsAndLimits
```

<Callout type="warning" title="VPA + HPA">
  VPA and HPA using CPU should not be used together on the same deployment. Use VPA to adjust resources and HPA with custom metrics, or just one of them.
</Callout>

## KEDA: Event-Driven Autoscaling

KEDA allows scaling based on events from various sources: queues, streams, metrics, cron, etc.

### KEDA Installation

```bash
helm repo add kedacore https://kedacore.github.io/charts
helm repo update

helm install keda kedacore/keda \
  --namespace keda \
  --create-namespace
```

### Scale to Zero

KEDA's differentiator is allowing scale to zero (0 replicas when there's no work):

```yaml
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: myapp-scaledobject
spec:
  scaleTargetRef:
    name: myapp
  minReplicaCount: 0   # Scale to zero!
  maxReplicaCount: 100
  cooldownPeriod: 300  # Time before scale to zero

  triggers:
    - type: prometheus
      metadata:
        serverAddress: http://prometheus:9090
        metricName: http_requests_total
        query: sum(rate(http_requests_total{app="myapp"}[2m]))
        threshold: "100"
```

### KEDA with AWS SQS

```yaml
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: sqs-consumer
spec:
  scaleTargetRef:
    name: sqs-consumer
  minReplicaCount: 0
  maxReplicaCount: 50

  triggers:
    - type: aws-sqs-queue
      authenticationRef:
        name: aws-credentials
      metadata:
        queueURL: https://sqs.us-east-1.amazonaws.com/123456789/my-queue
        queueLength: "5"  # 1 pod for every 5 messages
        awsRegion: us-east-1

---
apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: aws-credentials
spec:
  secretTargetRef:
    - parameter: awsAccessKeyID
      name: aws-secret
      key: access-key
    - parameter: awsSecretAccessKey
      name: aws-secret
      key: secret-key
```

### KEDA with Kafka

```yaml
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: kafka-consumer
spec:
  scaleTargetRef:
    name: kafka-consumer
  minReplicaCount: 1
  maxReplicaCount: 100

  triggers:
    - type: kafka
      metadata:
        bootstrapServers: kafka:9092
        consumerGroup: my-consumer-group
        topic: my-topic
        lagThreshold: "100"  # Scale when lag > 100
        offsetResetPolicy: earliest
```

### KEDA with Cron Schedule

```yaml
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: scheduled-scaler
spec:
  scaleTargetRef:
    name: myapp
  minReplicaCount: 1
  maxReplicaCount: 20

  triggers:
    # Scale up during business hours
    - type: cron
      metadata:
        timezone: America/Sao_Paulo
        start: 0 8 * * 1-5   # 8am, Mon-Fri
        end: 0 18 * * 1-5    # 6pm, Mon-Fri
        desiredReplicas: "10"

    # Scale down outside business hours
    - type: cron
      metadata:
        timezone: America/Sao_Paulo
        start: 0 18 * * 1-5
        end: 0 8 * * 1-5
        desiredReplicas: "2"
```

## Combined Strategies

### HPA + KEDA for Different Triggers

```yaml
# KEDA for scale to zero and event-based triggers
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: myapp-keda
spec:
  scaleTargetRef:
    name: myapp
  minReplicaCount: 0
  maxReplicaCount: 50
  triggers:
    - type: prometheus
      metadata:
        serverAddress: http://prometheus:9090
        query: sum(rate(http_requests_total{app="myapp"}[2m]))
        threshold: "50"

# HPA takes over when there's at least 1 replica (for CPU-based)
# Use only if KEDA doesn't cover your use case
```

## Cluster Autoscaler

Scales cluster nodes when pods cannot be scheduled:

```yaml
# For AWS EKS
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-autoscaler-config
data:
  parameters: |
    {
      "scale-down-delay-after-add": "10m",
      "scale-down-unneeded-time": "10m",
      "scale-down-utilization-threshold": "0.5",
      "skip-nodes-with-local-storage": "false",
      "expander": "least-waste"
    }
```

<Callout type="success" title="Complete Stack">
  **VPA** for initial right-sizing + **KEDA** for scale to zero and events + **Cluster Autoscaler** for nodes = maximum cost efficiency.
</Callout>

## Metrics to Monitor

```yaml
# Prometheus rules for autoscaling alerts
groups:
  - name: autoscaling
    rules:
      - alert: HPAMaxedOut
        expr: |
          kube_horizontalpodautoscaler_status_current_replicas
          == kube_horizontalpodautoscaler_spec_max_replicas
        for: 15m
        labels:
          severity: warning
        annotations:
          summary: "HPA {{ $labels.horizontalpodautoscaler }} at max replicas"

      - alert: HPANotScaling
        expr: |
          kube_horizontalpodautoscaler_status_desired_replicas
          != kube_horizontalpodautoscaler_status_current_replicas
        for: 15m
        labels:
          severity: warning
```

---

*Want to optimize autoscaling for your Kubernetes workloads? [Talk to our specialists](/en/contato) for a personalized analysis.*
