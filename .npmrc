# NPM Configuration for Integr8 Website

# Use pnpm as package manager
# npm_execpath=/usr/local/bin/pnpm

# Use exact versions for reproducibility
save-exact=true

# Strict SSL for security
strict-ssl=true

# Set registry
# registry=https://registry.npmjs.org/

# Auth tokens (use .npmrc.local for sensitive data)
